[build]
  # 1. Point Netlify to the exact folder where package.json lives
  base = "website/web"
  
  # 2. The command to build your Vite project
  command = "npm run build"
  
  # 3. The folder Vite generates inside website/web/
  publish = "dist"